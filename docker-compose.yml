services:
  bridge:
    build: .
    container_name: hypixel-bridge
    restart: unless-stopped
    volumes:
      - ./config.yaml:/app/config.yaml
      - ./config:/app/config
      - ./logs:/app/logs
      - ./plugins:/app/plugins
    environment:
      - NODE_ENV=production
